<h1>Salon</h1>

<p>Add a new stylist:</p>

<form action="/stylists" method="post">
  <label for="stylist_name">Enter stylist's name:</label>
  <input id="stylist_name" name="stylist_name" type="text">

  <button type="submit">Add stylist</button>
</form>

<p>Add a new client:</p>

<form action="/clients" method="post">
  <label for="stylist_id">Select a Stylist</label>
  <select id='stylist_id' name='stylist_id' type='text'>
    <% @stylists.each() do |stylist| %>
    <option value="<%= stylist.id()%>"> <%= stylist.stylist_name() %> </option>
    <% end %>
  </select>

  <label for="client_name">Enter client's name:</label>
  <input id="client_name" name="client_name" type="text">

  <button type="submit">Add client</button>
</form>

<% if @stylists.any?() %>
<p>Click on a stylist to see their clients (or edit/delete their info):</p>

<ul>
  <% @stylists.each do |stylist| %>
  <li><a href="/stylists/<%= stylist.id() %>"><%= stylist.stylist_name() %></a></li>
  <% end %>
</ul>
<% else %>
<p>No stylists yet!</p>
<% end %>
